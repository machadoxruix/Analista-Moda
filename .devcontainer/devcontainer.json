{
  "name": "Analista Moda",
  "postStartCommand": "sudo service apache2 start && sudo ln -sf /workspaces/Analista-Moda/* /var/www/html/ && gh codespace ports visibility 80:public -c $CODESPACE_NAME",
  "forwardPorts": [80],
  "portsAttributes": {
    "80": {
      "visibility": "public"
    }
  }
}